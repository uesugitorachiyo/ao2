# Risky PR Run

Provider-free MVP workflow proving AO2 governance:

- compile workflow
- deny risky git push
- exact-digest approve a narrow file write
- reject once for missing tests
- correct and pass verifier
- accept with evidence

