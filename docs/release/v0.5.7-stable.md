# AO2 v0.5.7 Stable Patch Release Notes

AO2 `v0.5.7` is the current stable patch release for the AO2 `v0.5` release
line. It was published from qualified source
`a3d8d19cef8f3aa69ea14e46ef94cc9706a502a7` with AO2 Control Plane `v0.1.18`
as its supported companion.

Release URL:
<https://github.com/uesugitorachiyo/ao2/releases/tag/v0.5.7>

## Changes Since v0.5.6

- Refreshes the execution-receipt compatibility evidence used by AO2 Control
  Plane `v0.1.18`.
- Adds canonical repository agent guidance while preserving the established
  coordination contract wording.
- Binds physical-Windows qualification rows to their worker provenance and
  exact qualified source.

## Supported Archives

- macOS aarch64
- Linux x86_64
- Windows x86_64

Linux aarch64 hosts may use the Linux x86_64 archive under explicit Docker
emulation. AO2 `v0.5.7` does not publish a Linux aarch64 archive.

The published five-asset set passed checksum and attestation verification,
native macOS aarch64, Linux x86_64, and Windows x86_64 installation and doctor
checks, credential-free governed fixtures, and explicit public-pair
verification. These are release qualification facts, not new publication or
deployment authorization.
