# Missed Call Recovery Fixture

Small local fixture used by AO2 factory app-run dogfood.

The target product is a missed-call revenue recovery workflow for local service
businesses. It scores missed callers, prioritizes hot leads, and drafts a
reply that can be reviewed before sending.

Verifier:

```bash
python -m pytest -q
```
